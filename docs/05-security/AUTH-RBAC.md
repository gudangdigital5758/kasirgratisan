# Authentication & RBAC

Admin target: Google OAuth with explicit allowlist/role mapping.

Customer authorization must be separate from Admin authorization.

Service-to-service access must use machine credentials and scoped permissions.
